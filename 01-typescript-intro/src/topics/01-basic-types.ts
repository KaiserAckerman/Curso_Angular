const name: string = 'Roberto';
let hpPoints: number | 'FULL' = 95;
const isAlive: boolean = true;
hpPoints = 'FULL';

console.log(name, hpPoints, isAlive);

export{}; // Para que no haya problemas con las variables de otros archivos

